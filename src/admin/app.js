const config = {
  locales: ["it"],
  // translations: {
  //   it: {
  //     "content-manager.containers.SettingPage.settings": "Impostazioni"
  //   }
  // }
};

const bootstrap = (app) => {
  console.log(app);
};

export default {
  config,
  bootstrap,
};
